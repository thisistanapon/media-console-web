# ============================================================================
# Media Console — Netlify configuration
# No build step. Netlify publishes the repository root as-is.
# ============================================================================

[build]
  publish = "."

# Serve /about/ from /about/index.html and strip trailing-slash inconsistency.
[build.processing]
  skip_processing = false

# --- Redirects --------------------------------------------------------------
# Force the www host so canonical URLs and analytics stay on one origin.

[[redirects]]
  from = "https://media-console.com/*"
  to = "https://www.media-console.com/:splat"
  status = 301
  force = true

# Old flat URLs, if any exist from the previous site, keep their equity.
# Add one block per legacy path as you find them in Search Console.
# [[redirects]]
#   from = "/services.html"
#   to = "/services/"
#   status = 301

# --- Headers ----------------------------------------------------------------

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Fingerprint-free assets: revalidate but allow long caching once stable.
[[headers]]
  for = "/assets/css/*"
  [headers.values]
    Cache-Control = "public, max-age=3600, must-revalidate"

[[headers]]
  for = "/assets/js/*"
  [headers.values]
    Cache-Control = "public, max-age=3600, must-revalidate"

[[headers]]
  for = "/assets/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/img/*"
  [headers.values]
    Cache-Control = "public, max-age=2592000"
